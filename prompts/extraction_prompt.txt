You are an expert research assistant. Extract stable, structured information from an academic paper.

Return ONLY valid JSON. Do not include Markdown, comments, or extra explanation.

Extraction mode: {extraction_mode}
Current section/chunk: {section_name}
Previous extraction error, if any: {previous_error}

Use this exact JSON schema:

{{
  "paper_id": "{paper_id}",
  "arxiv_id": "string",
  "venue_or_source": "string",
  "title": "string",
  "authors": ["string"],
  "year": "string",
  "abstract": "string",
  "task": "string",
  "method_name": "string",
  "contributions": ["string"],
  "research_problem": "string",
  "motivation": "string",
  "methodology": "string",
  "datasets": ["string"],
  "baselines": ["string"],
  "metrics": ["string"],
  "main_results": "string",
  "limitations": "string",
  "future_work": "string",
  "citation_context": "string",
  "related_papers": ["string"],
  "keywords": ["string"],
  "confidence": {{
    "datasets": 0.0,
    "baselines": 0.0,
    "metrics": 0.0,
    "methodology": 0.0
  }}
}}

Extraction rules:
- Prefer explicit information from the paper over guesses.
- If a field is not mentioned, use an empty string or an empty list.
- Use confidence values from 0.0 to 1.0 for field-level confidence.
- For section_chunk mode, extract only what is supported by this chunk but still return the full schema.
- Keep list items short and normalized, for example "CIFAR-10" instead of a long phrase.
- Do not invent datasets, baselines, metrics, or results.
- Focus on the paper's technical contribution, not a general summary.
- For methodology, describe the proposed model, algorithm, framework, or pipeline.
- For main_results, include concrete comparative findings when available.
- For citation_context, summarize how the paper positions or cites prior work when explicit.

Known metadata from arXiv:

{metadata_json}

Paper text:

{paper_text}
